spring:
  application:
    name: microservice-config-client    
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: microservice-config-server
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    
      
management:
  security:
    enabled: false     
 